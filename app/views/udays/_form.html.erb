<%= form_for(@uday) do |f| %>
  <% if @uday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uday.errors.count, "error") %> prohibited this uday from being saved:</h2>

      <ul>
      <% @uday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rno %><br>
    <%= f.number_field :rno %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
